# Example Repositories

`examples/good-service` is a compact service fixture with a healthy Dockerfile, GitHub Actions workflow, Kubernetes deployment, and Terraform configuration.

`examples/risky-service` is a compact service fixture with intentional deployment-readiness issues. It uses only fake placeholder values such as `example-token`, `example-password`, and `local-dev-only`.
